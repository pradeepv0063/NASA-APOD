# Uncomment the next line to define a global platform for your project
 platform :ios, '15.0'

target 'NASA APOD' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NASA APOD
  pod 'SwiftLint', :inhibit_warnings => true
  pod 'PromiseKit', "~> 6.8", :inhibit_warnings => true
  pod 'Kingfisher', '~> 7.0', :inhibit_warnings => true
  pod 'MBProgressHUD', '~> 1.2.0', :inhibit_warnings => true
  pod "youtube-ios-player-helper", :inhibit_warnings => true

end
